package main

/* import(
	"fmt"
)
func main() {
	var num1 int = 7
	//fallthrough
	//1)
	switch num1{
	case 6:
		fmt.Println("666")
	case 7:
		fmt.Println("777")
	default:
		fmt.Println("nonono")
	}

	//2)表达式（true false）-----进行相等判断的类型
    switch {
        case num1 < 0:
            fmt.Println("Number is negative")
        case num1 > 0 && num1 < 10:
            fmt.Println("Number is between 0 and 10")
        default:
            fmt.Println("Number is 10 or greater")
    }

	//3)带初始化
	//必须带分号！！！！
	switch first,second := 10,100;{
	case first < second:
		fmt.Println("first")
	case first <second:
		fmt.Println("second")
	default:
		fmt.Println("same")

	}
} */